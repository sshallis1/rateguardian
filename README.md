# Rate Guardian API

This repository contains the serverless functions and configuration files for the Rate Guardian engine. It exposes an `/api/recordHealth` endpoint that returns the health status and integrates with Supabase and GoHighLevel for monitoring mortgage rates.
