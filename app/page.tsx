export default function HomePage() {
  return (
    <main>
      <div className="card">
        <h1>Rate Guardian</h1>
        <p>Next.js App Router migration (V8) with hardened engine APIs.</p>
        <p>
          API endpoints available at <span className="code">/api/intake</span> and
          <span className="code"> /api/test-engine</span>.
        </p>
      </div>
    </main>
  );
}
