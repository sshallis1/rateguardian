{
  "name": "rateguardian",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "mkdir -p build && echo '<html><body>Rate Guardian API</body></html>' > build/index.html"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}

