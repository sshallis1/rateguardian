{
  "name": "rateguardian",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "vercel-build": "echo 'Skipping build step for API-only project'"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
